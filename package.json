{
  "private": true,
  "scripts": {
    "asbuild:untouched": "asc assembly/index.ts -b wasm/untouched.wasm -t wasm/untouched.wat --validate --sourceMap --debug",
    "asbuild:optimized": "asc assembly/index.ts -b wasm/optimized.wasm -t wasm/optimized.wat --validate --sourceMap --optimize",
    "asbuild": "npm run asbuild:untouched && npm run asbuild:optimized",
    "build": "yarn run clean && yarn run asbuild:optimized && webpack --mode production",
    "clean": "rm -rf build",
    "start": "yarn run clean && yarn run asbuild && webpack-dev-server --mode development"
  },
  "devDependencies": {
    "assemblyscript": "^0.9.2",
    "clean-webpack-plugin": "^3.0.0",
    "html-webpack-plugin": "^3.2.0",
    "webpack": "^4.41.6",
    "webpack-cli": "^3.3.11",
    "webpack-dev-server": "^3.10.3"
  }
}
